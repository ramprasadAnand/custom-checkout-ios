import XCTest
@testable import custom_checkout_ios

final class custom_checkout_iosTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(custom_checkout_ios().text, "Hello, World!")
    }
}
